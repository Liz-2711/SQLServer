Expression (char or nvarchar);Returns integer#Tinyint;Returns char(1)#Search expression (varchar);Expression to be searched (varchar);Start location (bigint);Returns integer or bigint#Expression (varchar);Expression (varchar);Returns integer#Expression (varchar or nvarchar);Number of characters (integer);Returns varchar or nvarchar#Expression (varchar or nvarchar);Returns integer or bigint#Expression (varchar or nvarchar);Returns varchar or nvarchar#Expression (varchar or nvarchar);Returns varchar or nvarchar#Expression (integer);Returns nchar(1)#Search pattern literal (varchar or nvarchar);Expression to search (varchar or nvarchar);Returns integer or bigint#Expression (nvarchar(128));(optional) Quote character (char(1));Returns nvarchar(258)#Expression to be searched (varchar or nvarchar);Search expression (varchar or nvarchar);Replacement expression (varchar or nvarchar);Returns varchar or nvarchar#Expression (varchar or nvarchar);Expression (bigint);Returns varchar or nvarchar#Expression (varchar or nvarchar);Returns varchar or nvarchar#Expression (varchar or nvarchar);Expression (bigint);Returns varchar or nvarchar#Expression (varchar or nvarchar);Returns varchar or nvarchar#Expression (varchar);Returns varchar#Expression (integer);Returns char#Expression (float);(optional) Length (integer, default);(optional) Scale (integer, default);Returns char#Input String (varchar or nvarchar);Separator (varchar or nvarchar);Returns table#Expression to be searched (varchar, nvarchar or varbinary);Starting position (bigint);Number of characters (bigint);Replacement expression (varchar, nvarchar or varbinary);Returns varchar, nvarchar or varbinary#Expression (varchar, nvarchar, text, ntext, varbinary or image);Starting position (bigint);Length (bigint);Returns varchar, nvarchar, text, ntext, varbinary or image#Expression (nvarchar);Returns integer#Expression (varchar or nvarchar);Returns varchar or nvarchar#Expression (varchar or nvarchar);Returns varchar or nvarchar#Separator (varchar or nvarchar);Expression (varchar or nvarchar);Expression (varchar or nvarchar);Returns varchar or nvarchar